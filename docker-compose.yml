services:
  frontend:
    image: ghcr.io/javid/ahnoud-frontend:latest
    container_name: ahnoud-frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production

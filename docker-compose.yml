services:
  frontend:
    image: ghcr.io/cavidyrm/ahnoud-frontend:latest
    container_name: ahnoud-frontend
    restart: unless-stopped
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
